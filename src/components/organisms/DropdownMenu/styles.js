export const styles = (theme) => ({
  mainContainer: {},
  menuButton: {
    backgroundColor: "transparent",
    "&:hover": {
      color: "#ffd8df",
      backgroundColor: "transparent",
    },
    margin: "0.3rem 0",
    padding: "0.5rem 2rem",
  },
});
